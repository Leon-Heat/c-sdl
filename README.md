# Compile
Make sure you have SDL2.0 library installed on your system and specify them in your compiler.  
To run the code, compile c with c compiler like gcc, tcc or any other ide-like environment.  
Example for gcc:  
gcc coordinate.c -lSDL2
